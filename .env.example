# Local defaults work without this file. Production secrets belong in Wrangler secrets.
API_ORIGIN=http://127.0.0.1:8787
# Production: set ENCRYPTION_KEY to a base64-encoded random 32-byte key on BOTH Workers.
# WEB_ORIGIN=https://usemack.ai
# GATEWAY_URL=https://mcp.usemack.ai/mcp

# Optional, server-only: export before starting local dev. Never prefix with NEXT_PUBLIC_.
# AI_GATEWAY_API_KEY=
